# 从 @BotFather 获取机器人 Token
TG_BOT_TOKEN=your_bot_token_here

# 机器人主人的 Telegram User ID (支持多个用逗号分隔)
# 可以通过 @userinfobot 获取自己的 User ID
TG_BOT_OWNER_ID=123456789

# 数据库路径 (可选，默认为 data/bot.db)
TG_BOT_DB_PATH=data/bot.db

# ===== LLM API 配置 =====

# 支持的提供商: openai, claude, gemini, custom
LLM_PROVIDER=openai

# API 密钥
LLM_API_KEY=your_llm_api_key

# API 基础 URL (可选，用于自定义端点)
# OpenAI 默认: https://api.openai.com/v1
# Claude 默认: https://api.anthropic.com/v1
# Gemini 默认: https://generativelanguage.googleapis.com/v1beta
LLM_API_BASE=

# 模型名称
# OpenAI: gpt-3.5-turbo, gpt-4, gpt-4-turbo 等
# Claude: claude-3-haiku-20240307, claude-3-sonnet-20240229 等
# Gemini: gemini-1.5-flash, gemini-1.5-pro, gemini-pro 等
LLM_MODEL=gpt-3.5-turbo

# 生成参数 (可选)
LLM_MAX_TOKENS=1000
LLM_TEMPERATURE=0.7

# ===== Linux.do 截图功能配置 =====

# 全局默认 Token (可选，用户可通过命令设置自己的 Token)
# 获取方式: 登录 linux.do -> F12 开发者工具 -> Application -> Cookies -> _t 的值
LINUXDO_API_TOKEN=

# 功能总开关 (true/false)
LINUXDO_ENABLED=true
