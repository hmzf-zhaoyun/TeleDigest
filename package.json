{
  "name": "teledigest-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:oneclick": "node scripts/oneclick-deploy.mjs",
    "db:init": "wrangler d1 execute DB --file schema.sql",
    "db:reset": "wrangler d1 execute DB --file schema.sql --reset"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
